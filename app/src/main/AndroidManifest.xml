<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="pl.media30.zamowieniapubliczne" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".Activities.MainActivity"
            android:configChanges="orientation|screenSize"
            android:label="@string/title_activity_main" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEARCH" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Activities.InfoActivity"
            android:label="@string/title_activity_info" >
        </activity>
        <activity
            android:name=".Activities.ZamowienieActivityFragment"
            android:label="@string/title_activity_zamowienie" >
        </activity>
        <activity
            android:name=".Activities.SearchActivity"
            android:label="@string/search" >
        </activity>
        <activity
            android:name=".Activities.WykresyActivity"
            android:label="@string/title_activity_wykresy" >
        </activity>
        <activity
            android:name=".Activities.UlubioneActivity"
            android:label="@string/title_activity_ulubione" >
        </activity>
    </application>

</manifest>
